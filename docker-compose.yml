version: '3.3'

services:
    od-api:
        build: 
          context: .
          dockerfile: Dockerfile
        ports:
            - '5000:5000'
        image: od-api
        container_name: od-api-qs4paul